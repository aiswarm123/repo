services:
  bot:
    build: .
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      # Persist the SQLite database across container restarts
      - bot_data:/app/data
    environment:
      DATABASE_PATH: /app/data/support.db

volumes:
  bot_data:
